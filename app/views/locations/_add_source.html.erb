<strong>Add a source:</strong>

  <%= form_for([@location, @location.location_sources.build]) do |f| %>
    <%= f.collection_select(:source_id, Source.order(:name).all, :id, :name) %>
    Page(s)
    <%= f.text_field :pages %>
    <%= f.submit "Add"%>
  <% end %>

<br>
