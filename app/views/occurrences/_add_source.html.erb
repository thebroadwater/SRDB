<strong>Add a source:</strong>

  <%= form_for([@occurrence, @occurrence.occurrence_sources.build]) do |f| %>
    <%= f.collection_select(:source_id, Source.order(:title).all, :id, :title) %>
    Page(s)
    <%= f.text_field :pages %>
    <%= f.submit "Add"%>
  <% end %>
<br>
